commands:
  - name: "Disk Usage"
    command: "df -h"
    repeat: 5  # Repeat every 5 seconds (0 means run once)
  - name: "Memory Usage"
    command: "free -h"
    repeat: 10
  - name: "List Processes"
    command: "ps aux"
    repeat: 0  # Run once

